# Configuração do Banco de Dados
# Opções: sqlite_memory (desenvolvimento rápido) | sqlite_file (persistente) | postgres (produção)
DB_TYPE=sqlite_memory
SQLITE_DB_PATH=":memory:"
# Para banco persistente, use: SQLITE_DB_PATH="giropro.db"
# Para PostgreSQL, use: DATABASE_URL="postgresql://user:password@localhost:5432/giropro"

# Configurações da API
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Segurança - JWT
# IMPORTANTE: Gere chaves secretas únicas para produção!
# Você pode usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="giropro_jwt_secret_key_2024"
JWT_REFRESH_SECRET="giropro_refresh_secret_key_2024"
JWT_EXPIRES_IN="7d"

# Bcrypt
BCRYPT_ROUNDS=12

# CORS - Origens permitidas (separadas por vírgula)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:19006,http://localhost:8081"

# Logs
LOG_LEVEL=debug

# Email (opcional - para envio de emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-de-app
# EMAIL_FROM=noreply@giropro.com
